# Debug Journal

---

## Bug 1 — Silent RLS Block

| Field          | Your Entry                                                                                                                                                                                                                                                                       |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Symptom**    | When opening the /dashboard page, the shipments table renders normally but displays `“No Data Found”` instead of shipment records. The page loads without errors and the request to fetch shipments appears to complete successfully. However, checking the Supabase Table Editor confirms that the shipments table contains 5 existing rows, indicating that the frontend is not receiving any visible data despite the database having records. |
| **Hypothesis** | **Observations:**<br>• The dashboard loads normally but shows "No Data Found" in the shipments table.<br>• The Supabase Table Editor confirms that the shipments table contains 5 rows.<br>• The frontend query does not produce an error.<br><br>**Possible explanations:**<br>1. Row Level Security (RLS) may be blocking read access for the anon role used by the frontend client.<br>2. The query may be executing successfully but returning no rows because no `SELECT` policy exists.<br>3. The frontend may not have permission to read rows even though the table contains data. |
| **AI Prompt**  | I'm debugging a Supabase table where RLS is enabled, and only see an update policy defined. My frontend query returns no rows even though the table clearly has data. Do I also need to define an explicit select policy for reads, or is there a default policy that allows it? |
| **Fix**        | • Updated at: Supabase SQL Editor (table: shipments)<br>• The issue was caused by Row Level Security (RLS) blocking read access because no `SELECT` policy existed.<br>• Added a new RLS policy to allow the frontend client to read rows while keeping RLS enabled: |                                                    

### SQL Fix

````sql
create policy "Allow anon select"
on shipments
for select
using (true);

````
---

## Bug 2 — Ghost Mutation

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom**    | When changing a shipment’s status using the dropdown, the UI displays a success toast, but after refreshing the page, the status reverts to its previous value. This happens for all status transitions (Pending -> In Transit, In Transit -> Delivered, etc.), meaning the UI shows success even though the database never updates.|
| **Hypothesis** | **Observations:**<br>• UI always shows a success notification after updating a shipment.<br>• Refreshing restores the original status.<br>• No errors in the browser console.<br>• The database does not reflect the changes.<br><br> **Possible explanations:** <br>1. The Supabase update query is not awaited, so the server action returns success before the mutation completes.<br>2. The mutation result is not checked, so errors are silently ignored.<br>3. The query might target the wrong row (minor, verified and ruled out).<br>4. RLS policies could restrict `UPDATE` operations (checked and confirmed not the issue). |
| **AI Prompt**  |I am debugging a server action. When I update a shipment status, it always returns success, but the value doesn't persist after refresh. What could cause an update mutation to silently fail? If a Supabase update call isn't awaited inside an async function, could the function return before the mutation completes?|
| **Fix**        | • Updated file: `src/actions/update-status.ts`<br>• Added await to the Supabase update query so the mutation completes before returning.<br>• Extracted { error } from the Supabase response to detect any failures.<br>• `Return { success: true }` only if the update `succees;` otherwise, `return { success: false, error: error.message }`.<br>• Call `revalidatePath("/dashboard")` after a successful update to ensure the UI reflects the latest data. |
---

## Bug 3 — The Invisible Cargo

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom**    | When opening the `/dashboard` page causes the UI to freeze, and the server logs show hundreds of repeated `GET /dashboard` requests per second. The page becomes unresponsive within moments, even though the user is not interacting with the page. This indicates that some client-side code is repeatedly triggering navigation or rerenders, creating an infinite loop.       |
| **Hypothesis** | **Hypothesis / Possible explanations:**<br>• The `useEffect` in the DataTable component synchronizes the sorting state with the URL using `router.push()`.<br>• The dependency array currently references `table.getState().sorting`, which produces a new reference on every render, causing the effect to run continuously.<br>• Including searchParams in the dependency array may also retrigger the effect every time the URL changes.<br>• Each execution of `router.push()` causes a rerender -> effect runs again -> infinite rerender loop.<br><br>**Investigation:**<br>• Inspected `src/components/data-table.tsx`.<br>• The `useEffect` responsible for updating the URL with the table’s sorting state kept firing because `table.getState().sorting` produces a new reference on every render.<br>• Each render executed `router.push('/dashboard?...')`, causing the component to rerender repeatedly and the effect to run again ->  infinite loop.<br>• Root cause: effect depended on an unstable value, combined with router navigation, creating continuous rerenders.   |
| **AI Prompt**  |  For bug 3, I'm debugging the dashboard page. Opening `/dashboard` causes the page to freeze, and the server logs show hundreds of repeated GET /dashboard requests, even without user interaction.<br>In `src/components/data-table.tsx`, a `useEffect` syncs the table’s sorting state to the URL using `router.push()`. The current dependency array includes `table.getState().sorting, searchParams, and router`. ESLint shows a `warning about a missing dependency: sorting`.<br><br>Observed issues and questions:<br>1. Could referencing `table.getState().sorting` in the dependency array cause the effect to run on every render due to unstable references?<br>2. If I change the dependency to use the stable sorting state directly, would that prevent the rerender loop?<br> 3. Including `searchParams` in the dependency array seems to retrigger the effect every time the URL changes. Is this causing the infinite navigation loop?<br> 4. What is the recommended way to sync sorting state with the URL in Next.js 15 without causing infinite rerenders or router navigation loops? Are there best practices for using `router.push()` inside a useEffect?  | 
| **Fix**        | • Updated file: `src/components/data-table.tsx)`<br>• `useEffect` dependency array changed to `[sorting]` -> effect only runs when sorting state changes.<br>• Removed `searchParams` from dependency array -> prevents effect from retriggering on URL updates.<br>• Sorting state still correctly syncs with URL without causing infinite rerenders. |

---

## Bug 4 —  The Invisible Cargo

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom**    | When opening the /dashboard page, all shipment rows appear correctly, but the Cargo column is mostly blank, only displays the weight unit `(kg)`. Only rows with no cargo data correctly show a dash `(—)`. The page loads without errors, but the item names and weights for cargo are not rendered, even though the database contains valid cargo records. This makes it seem like cargo data is missing from the dashboard.        |
| **Hypothesis** | **Hypothesis / Possible explanations:**<br>• The Cargo column renderer expects `cargo_details` to be a single object, but Supabase returns an array of objects.<br>• Accessing `.item` and `.weight_kg` on the array directly causes undefined values, resulting in blank UI cells.<br>• The TypeScript Shipment type does not match the Supabase response, which leads to type errors and incorrect rendering.<br>• Null values in the database are correctly handled, but the UI logic fails for actual cargo data due to the mismatch.<br><br>**Investigation:**<br>• Checked the Supabase response: `cargo_details` is returned as an array, e.g., `[ { item: "Steel Pipes", weight_kg: 120 } ]`.<br>• Current `DataTable` cell code accesses `cargo.item` directly -> undefined because cargo is actually an array.<br>• TypeScript type for Shipment defines `cargo_details` as `{ item: string; weight_kg: number }`  `null` -> mismatch with real data.<br>• Rows with null cargo still render correctly (dash), confirming the problem is only with arrayed cargo data. |
| **AI Prompt**  | For bug 4, I'm debugging the dashboard page in a Next.js 15 application that uses Supabase and TanStack Table. The Cargo column in the shipments table shows inconsistent data: for most rows, the UI only displays the weight unit `("kg")` or is blank, even though the database contains valid cargo information. Rows with null cargo correctly display a dash `(—)`.<br><br>Observed issues and questions:<br>• Console logging shows that `cargo_details` returned by Supabase is an array of objects, for example `[ { item: "Laptop Batch A", weight_kg: 120 } ]`. The current DataTable cell renderer expects a single object `({ item: string; weight_kg: number }` `null)`. Could this mismatch between the database response and the expected type cause the UI to render incomplete cargo information?<br>• Should I update the TypeScript Shipment type to reflect cargo_details as an array instead of a single object? How can this be done safely while maintaining type safety in the table renderer?<br>• How can I normalize the cargo array so that the table always displays the first item correctly, including both `item` and `weight_kg`, without affecting rows with null cargo?<br>• Are there best practices for handling array to object normalization for table components in Next.js 15 using Supabase, to ensure the UI always renders data consistently?<br>• How can I make sure that this fix doesn’t break any existing components or assumptions elsewhere in the dashboard?
| **Fix**        |• Updated  src/app/dashboard/page.tsx<br>• Updated the cargo data handling logic to normalize the structure returned from Supabase.<br>• Mapped shipment records through a normalizeCargoDetails helper before passing to the DataTable.<br>• Preserved null cargo values so rows without cargo still render the dash placeholder.<br>• Cargo names and weights now display correctly in the table, and sorting/rendering behavior remains stable.  |

---

## Bug 5 — The Unreliable Search

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom**    |  • Typing quickly in the dashboard search input produces inconsistent results.<br>• For example, typing `"Medical"` may temporarily display unrelated shipments or even all rows instead of only the expected filtered results.<br>• The issue becomes more noticeable when simulating slow network conditions using Chrome DevTools → Network → Slow 3G..<br><br>Example server logs when typing quickly:<br>`POST /dashboard`<br>. . .<br>`POST /dashboard`
| **Hypothesis** |  **Possible Explanation:**<br>• The search input triggers a request on every key press via the `onChange` handler.<br>• Each keystroke immediately calls the `searchShipments` server action.<br>• Multiple asynchronous requests are sent before earlier ones finish.<br>• Under network latency, responses may return in a different order.<br>Because the UI updates the table whenever a response resolves, an earlier request may overwrite the results of a newer query.<br><br>**Investigation:**<br>•  Inspected `src/components/ui/data-table.tsx`.<br>The search handler was implemented as the handleSearch function, which is  bound directly to the input’s `onChange` event.<br>As a result:<br>•  Every keystroke sends a new asynchronous request.<br>•  Multiple requests run concurrently while the user is still typing.<br>•  Under slow network conditions, responses may return out of order.<br>•  The UI updates immediately when any response resolves.<br><br>**Root cause:** the search logic executes on every keypress without any throttling or debouncing, allowing overlapping requests and inconsistent UI updates.
| **AI Prompt**  | While debugging Bug 5, I investigated the search input in `src/components/ui/data-table.tsx`. The input triggers the following function on every `onChange` event in the `handleSearch` function.<br><br>Observed behavior:<br>- Every keystroke sends a new request to the `searchShipments` server action.<br>- Typing quickly produces multiple `POST /dashboard` requests in the server logs.<br><br>Questions:<br>- Is triggering a request on every keypress the main cause of the excessive API calls?<br>- What is the recommended pattern to prevent API requests from firing on every keystroke in a search input?<br>- Would implementing a debounce mechanism for the search handler be the correct approach?<br>- How can a debounce helper be typed correctly in TypeScript strict mode without using any?    
| **Fix**        |  Updated the search implementation in `src/components/ui/data-table.tsx` to prevent excessive API requests during rapid typing.<br>• Introduced a `runSearch` function responsible for executing the search request and updating the table state.<br>• Added a small debounce helper function inside the same file to delay execution of the search call.<br>• Used `useMemo` to create a stable debounced version of `runSearch`, preventing the function from being recreated on every render.<br>• Updated the search input `onChange` handler to call the debounced function instead of triggering the API request directly.  |

---

## Bug 6 — The Persistent Ghost

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom**    | When updating the shipment status from `“Delivered”` to `“Pending”`, a green success toast appears, indicating the update was successful. However, after refreshing the page, the status remains `“Delivered”`, meaning no change was actually saved in the database. Other status transitions work normally. The issue only happens when changing `Delivered -> Pending.`        |
| **Hypothesis** | **Hypothesis (initial assumption):**<br>- There might be a database rule or policy in Supabase that prevents a shipment that is already Delivered from being reverted back to Pending.<br>- However, the frontend function `handleStatusUpdate` always shows a success toast without verifying whether the database update actually succeeded.<br>- As a result, the UI gives a false success message even though the database rejected the update.<br><br>**Investigation:**<br>- Added console logging inside the `updateShipmentStatus` server action to inspect the response returned by Supabase.<br>- After attempting the `Delivered -> Pending` transition again, the server console showed the following error:<br>`Supabase error: Cannot revert a delivered shipment to pending status`<br>- This confirmed that Supabase has a constraint or policy that blocks this status transition.<br>- The actual issue was not the update query itself, but the frontend not checking whether the update succeeded or failed.<br><br>**Root Cause:**<br>The function `updateShipmentStatus` always returned `{ success: true }`, even when Supabase returned an error.Because of that, the frontend always displayed a success toast, even when the database operation failed.   |
| **AI Prompt**  |  While debugging Bug 6, I investigated the shipment status update flow. When attempting to update a shipment from `Delivered -> Pending`, the UI displayed a success toast, but the database status did not change after refreshing the page.<br><br>The frontend calls the `updateShipmentStatus(id, status)` server action. However, nothing was logged in the server console during the operation, so I wanted to inspect what Supabase actually returns when the update is executed.<br><br>Questions:<br>• How can I add logging inside updateShipmentStatus to inspect the `data` and `error` returned by Supabase without changing the existing logic?<br>• What is the best way to log responses such as Supabase error or updated rows from the update query?<br><br>After adding logging, I observed the following error in the server console when attempting `Delivered -> Pending`:<br>`Supabase error: Cannot revert a delivered shipment to pending status`<br>This indicates that Supabase prevents this transition, but the frontend still shows a success toast.<br><br>Follow-up questions:<br>• Why does the frontend show a success toast even when the database update fails?<br>• How can the server action return a structured response such as `{ success: boolean; error?: string }` to indicate whether the update actually succeeded?<br>• How should handleStatusUpdate in `columns.tsx` read this response and display either a `success toast` or `error toast`, while keeping the code changes minimal? |
| **Fix**        |  • Add logging inside `updateShipmentStatus`<br>Add console.log statements in the server action to inspect the data and error returned by Supabase during the update operation.<br><br>• Return a structured response from the server action<br>Update `updateShipmentStatus` to return a structured object such as:<br>`{ success: false, error: error.message }`<br>This allows the frontend to determine whether the database update actually succeeded or failed.<br><br>• Update the frontend handler `(handleStatusUpdate)`<br>Modify the function to check the success property from the server response:<br>`If success = true` -> show a success toast<br>`If success = false` -> show an error toast with the returned error message |
````
